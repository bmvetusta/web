---
interface Props {
  videoId?: string;
  playlistId?: string;
}

if (!Astro.props.videoId && !Astro.props.playlistId) {
  return null;
}
---

<script>
  import '@justinribeiro/lite-youtube';
</script>

<lite-youtube {...Astro.props}></lite-youtube>
